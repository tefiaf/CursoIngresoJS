function mostrar()
{
    //las variables fuera del for no existe
    //primera parte declara la variable, lo segundo pone la condicion, ejecuta la condicion, ejecuta el codigo
    //y modifica
    // puedo no poner nada adentro de los parentesis pero si o si tienen que estar los punto y  coma
    var cont =0;
    for (cont=1;cont<11;cont++){
        document.write(cont);
    }
}